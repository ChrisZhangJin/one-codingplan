---
phase: 6
slug: web-portal
status: approved
reviewed_at: 2026-04-17
shadcn_initialized: false
preset: none
created: 2026-04-17
---

# Phase 6 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (requires init before build — run `npx shadcn init` in `web/`) |
| Preset | none yet — use shadcn neutral default until operator configures one |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react (bundled with shadcn) |
| Font | Inter (shadcn default via Tailwind font-sans) |

**shadcn gate note:** `components.json` not found. `web/` directory does not yet exist. Before executing, run:
```
cd web && npx shadcn init
```
Accept defaults (TypeScript, Tailwind, src/ layout). Use the neutral color theme.

**Registry:** shadcn official only. No third-party registries. Registry safety gate: not required.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, status dot margins, inline badge padding |
| sm | 8px | Button padding inline, form field gap, table cell padding-x |
| md | 16px | Card padding, form group spacing, section gap within a panel |
| lg | 24px | Panel padding, modal content padding |
| xl | 32px | Page content max-width padding, login form outer margin |
| 2xl | 48px | Vertical centering offset for login card |
| 3xl | 64px | Reserved — not used in this phase |

Exceptions: touch targets on block/unblock toggle buttons minimum 44px height (accessibility).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label | 12px | 400 (regular) | 1.4 |
| Heading | 20px | 600 (semibold) | 1.2 |
| Display | 28px | 600 (semibold) | 1.2 |

**Notes:**
- Body (14px/400) is the default for table rows, form inputs, and paragraph text. Source: functional-over-decorative principle from CONTEXT.md.
- Label (12px/400) is used for table column headers, form field labels, status badge text, and empty state sub-copy. The 12px size alone differentiates Label from Body; no weight distinction is needed.
- Heading (20px/600) is used for section titles ("Upstream Status", "Access Keys").
- Display (28px/600) is used only for the login card title ("one-codingplan").

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | hsl(0 0% 100%) — white (light) / hsl(222 47% 11%) — dark | Page background and all content surfaces |
| Secondary (30%) | hsl(210 40% 96%) — light / hsl(217 33% 17%) — dark | Cards, sidebar/nav background, table row hover |
| Accent (10%) | hsl(221 83% 53%) — blue | Specific elements only — see list below |
| Destructive | hsl(0 84% 60%) — red | "Block key" button and its confirmation only |

**Accent reserved for:**
- "Create Key" primary button
- "Login" submit button
- Active/healthy upstream status indicator dot
- Table row action: "Unblock Key" button (re-enabling a blocked key)

**Status color extensions (semantic, not structural):**
- Healthy upstream: accent blue dot + "Healthy" label
- Cooldown upstream: hsl(38 92% 50%) amber dot + "Cooldown" label
- Dead/disabled upstream: hsl(0 0% 60%) gray dot + "Disabled" label
- Key blocked: hsl(0 84% 60%) red badge text

**Default mode:** Light. No dark mode toggle required in this phase (operator console, not public UI).

---

## Component Inventory

Shadcn components to install:

| Component | `npx shadcn add` command | Used for |
|-----------|--------------------------|---------|
| Button | `button` | Login submit, Create Key, Block, Unblock Key, Enable/Disable toggle |
| Input | `input` | Login key field, Create Key form fields |
| Label | `label` | Form field labels |
| Card | `card` | Login form container, Upstream status card per provider |
| Table | `table` | Access keys table |
| Badge | `badge` | Key status (Active / Blocked), upstream health state label |
| Dialog | `dialog` | Create Key modal, Block key confirmation dialog |
| Separator | `separator` | Section dividers between upstream status and key table |
| Switch | `switch` | Upstream enabled/disabled toggle per provider row |
| Skeleton | `skeleton` | Loading state for key table and upstream list |
| Sonner | `sonner` | Success/error toast for key creation and API errors |

No third-party blocks. All from shadcn official registry.

---

## Layout Contract

### Login Page
- Full viewport, vertically and horizontally centered.
- Single `Card` (width: 360px fixed) containing:
  - Display title: "one-codingplan" (28px/600)
  - Subtitle: "Enter your admin key to continue" (14px/400, muted text)
  - `Input` of type `password` with label "Admin Key"
  - `Button` (full width, accent) with label "Sign in"
- On submit: POST to `GET /api/keys` with Bearer token as validation probe. On 200, store key in localStorage and navigate to dashboard. On 401, show inline error: "Invalid admin key."
- No username field. Source: CONTEXT.md specifics.

### Dashboard Page (authenticated)
Single-page layout, no tabs, no sidebar. Two stacked sections separated by a `Separator`.

**Section 1: Upstream Status**
- Section heading: "Upstream Status" (20px/600)
- Refresh button (ghost, icon only — `RefreshCw` from lucide-react) next to heading. `aria-label="Refresh upstream status"`. Manual refresh on click. No auto-polling. Source: Claude's Discretion — fetch-on-mount + manual refresh is sufficient for an operator console.
- Grid of provider cards (2 columns on desktop, 1 on mobile): each `Card` shows:
  - Provider name (14px/400)
  - Status badge (Healthy / Cooldown / Disabled)
  - `Switch` for enabled/disabled toggle (calls `PATCH /api/upstreams/:name` or `POST /api/upstreams/rotate` as appropriate)
  - Status dot (colored per status color spec above)

**Section 2: Access Keys**
- Section heading: "Access Keys" (20px/600)
- "Create Key" button (accent, right-aligned to heading row)
- `Table` with columns: Name, Token (masked), Status, Budget, Expires, Usage (tokens), Actions
- Action cells: "Block Key" (destructive outline) / "Unblock Key" (accent outline) toggle + "Details" icon button (`aria-label="View key details"`, ghost variant)
- Empty state when no keys exist: see Copywriting Contract below.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Login page title | "one-codingplan" |
| Login page subtitle | "Enter your admin key to continue" |
| Login submit button | "Sign in" |
| Login invalid key error | "Invalid admin key. Check your admin_key config value." |
| Dashboard section: upstreams | "Upstream Status" |
| Dashboard section: keys | "Access Keys" |
| Primary CTA | "Create Key" |
| Create Key modal title | "Create Access Key" |
| Create Key modal submit | "Create Key" |
| Create Key success toast | "Key created. Copy it now — it won't be shown again." |
| Empty state heading (keys) | "No access keys" |
| Empty state body (keys) | "Create your first key to start proxying requests." |
| Error state (API unreachable) | "Could not reach the server. Check that ocp is running." |
| Error state (401 mid-session) | Redirect to login — no toast, no intermediate page. Source: CONTEXT.md D-03. |
| Block key confirmation title | "Block this key?" |
| Block key confirmation body | "Blocked keys receive 401 on every request. You can unblock at any time." |
| Block key confirmation button | "Block Key" |
| Unblock key button label | "Unblock Key" |
| Delete key — not in scope | Key deletion is a Management API capability (DELETE /api/keys/:id) but no portal UI for delete is required by PORT-02. Omit. |
| Upstream enable toggle (on) | "Enabled" |
| Upstream enable toggle (off) | "Disabled" |
| Upstream health: healthy | "Healthy" |
| Upstream health: cooldown | "Cooldown" |
| Upstream health: dead | "Unavailable" |
| Create Key name field placeholder | "e.g. claude-code-laptop" |
| Create Key budget field placeholder | "0 = unlimited" |
| Refresh button aria-label | "Refresh upstream status" |
| Details icon button aria-label | "View key details" |

---

## Interaction Contract

### Auth Flow
1. User lands on `/` — check localStorage for `ocp_admin_key`.
2. If absent: render Login page.
3. If present: render Dashboard (key validated lazily on first API call).
4. Any `/api/*` response returning 401: `localStorage.removeItem('ocp_admin_key')` + `window.location.href = '/'`. Source: CONTEXT.md D-03.

### Key Create Flow
1. Click "Create Key" button.
2. Dialog opens with form: Name (required), Token Budget (optional, number), Allowed Upstreams (optional, comma-separated), Expires At (optional, date), Rate Limit/min (optional), Rate Limit/day (optional).
3. Submit: POST `/api/keys`. On success: show full token in a read-only input with copy button + success toast. Close dialog. Refresh key table.
4. Token is shown ONCE in the success dialog. Source: Phase 5 CONTEXT.md D-13.

### Block/Unblock Flow
1. Click "Block Key" in table action cell.
2. Confirmation dialog opens (see Copywriting Contract).
3. Confirm: POST `/api/keys/:id/block`. Refresh table row. Badge changes to "Blocked".
4. "Unblock Key" in same cell: no confirmation — direct POST `/api/keys/:id/unblock`. Refresh table row.

### Upstream Enable/Disable Toggle
1. Switch in upstream card triggers `POST /api/upstreams/:name/disable` or `enable` (TBD per exact Phase 5 API surface — use the route from Phase 5 D-16).
2. Optimistic UI: toggle state updates immediately, reverts on error + shows error toast.

---

## Data Refresh Strategy

- **Fetch on mount** for both upstreams and keys (single initial load).
- **Manual refresh** button on Upstream Status heading for health state.
- **Invalidate and refetch** after any mutating action (create key, block, unblock, toggle upstream).
- No polling, no websockets. Source: Claude's Discretion — operator console at personal scale does not require live push updates.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | button, input, label, card, table, badge, dialog, separator, switch, skeleton, sonner | not required |

No third-party registries declared.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
