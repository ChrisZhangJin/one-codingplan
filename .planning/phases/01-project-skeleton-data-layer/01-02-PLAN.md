---
phase: 01-project-skeleton-data-layer
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internal/server/server.go
  - internal/server/server_test.go
  - cmd/ocp/main.go
autonomous: true
requirements:
  - UPST-01
  - USGR-02

must_haves:
  truths:
    - "Binary starts without error and serves HTTP"
    - "GET /health returns 200 with JSON body containing status ok"
    - "Startup sequence: load config -> open DB -> migrate -> sync upstreams -> start server"
    - "Binary accepts --config flag to specify config file path"
  artifacts:
    - path: "internal/server/server.go"
      provides: "Gin engine setup with health endpoint"
      exports: ["NewEngine"]
    - path: "cmd/ocp/main.go"
      provides: "Application entry point wiring all packages"
      contains: "func main()"
  key_links:
    - from: "cmd/ocp/main.go"
      to: "internal/config/config.go"
      via: "config.Load call"
      pattern: "config\\.Load"
    - from: "cmd/ocp/main.go"
      to: "internal/database/database.go"
      via: "database.Open, Migrate, SyncUpstreams calls"
      pattern: "database\\.Open"
    - from: "cmd/ocp/main.go"
      to: "internal/server/server.go"
      via: "server.NewEngine call"
      pattern: "server\\.NewEngine"
---

<objective>
Create the Gin HTTP server with health endpoint and wire everything together in main.go so the binary boots, loads config, initializes the database, syncs upstreams, and serves HTTP.

Purpose: Delivers a runnable binary -- the Phase 1 success criteria require `go run ./cmd/ocp` to start and respond to `GET /health`.
Output: Bootable binary with full startup sequence and health check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-skeleton-data-layer/01-CONTEXT.md
@.planning/phases/01-project-skeleton-data-layer/01-RESEARCH.md
@.planning/phases/01-project-skeleton-data-layer/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- packages the executor will import -->

From internal/config/config.go:
```go
func Load(configPath string) (*Config, error)

type Config struct {
    Server    ServerConfig     `mapstructure:"server"`
    Database  DatabaseConfig   `mapstructure:"database"`
    Upstreams []UpstreamConfig `mapstructure:"upstreams"`
}
type ServerConfig struct {
    Port     int    `mapstructure:"port"`
    AdminKey string `mapstructure:"admin_key"`
}
type DatabaseConfig struct {
    Path string `mapstructure:"path"`
}
type UpstreamConfig struct {
    Name    string `mapstructure:"name"`
    BaseURL string `mapstructure:"base_url"`
    APIKey  string `mapstructure:"api_key"`
    Enabled bool   `mapstructure:"enabled"`
}
```

From internal/database/database.go:
```go
func Open(dbPath string) (*gorm.DB, error)
func Migrate(db *gorm.DB) error
func SyncUpstreams(db *gorm.DB, cfgUpstreams []config.UpstreamConfig) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gin server package with health endpoint</name>
  <files>internal/server/server.go, internal/server/server_test.go</files>
  <read_first>
    - .planning/phases/01-project-skeleton-data-layer/01-RESEARCH.md (Pattern 1: Gin Engine Setup)
    - .planning/phases/01-project-skeleton-data-layer/01-CONTEXT.md (D-02: Gin v1.10.x)
  </read_first>
  <behavior>
    - TestHealthEndpoint_Status200: Create engine via NewEngine(). Use httptest.NewRecorder + httptest.NewRequest for GET /health. Assert status code == 200.
    - TestHealthEndpoint_JSONBody: Same request. Parse response body as JSON. Assert body contains key "status" with value "ok".
    - TestHealthEndpoint_ContentType: Same request. Assert Content-Type header contains "application/json".
  </behavior>
  <action>
    1. Create `internal/server/server.go` with package `server`:
       - Import `net/http`, `github.com/gin-gonic/gin`
       - `func NewEngine() *gin.Engine`:
         - Call `gin.New()` (per RESEARCH.md: prefer gin.New over gin.Default for explicit middleware control)
         - Add `gin.Logger()` middleware
         - Add `gin.Recovery()` middleware
         - Register `GET /health` handler that returns `c.JSON(http.StatusOK, gin.H{"status": "ok"})`
         - Return the engine

    2. Create `internal/server/server_test.go` with the three test cases from behavior block:
       - Use `net/http/httptest` for request/response recording
       - Create engine via `server.NewEngine()`
       - Create request: `httptest.NewRequest(http.MethodGet, "/health", nil)`
       - Create recorder: `httptest.NewRecorder()`
       - Call `engine.ServeHTTP(w, req)`
       - Assert w.Code == 200
       - Parse w.Body with `encoding/json` into map[string]string, assert `m["status"] == "ok"`
       - Assert w.Header().Get("Content-Type") contains "application/json"

    3. Run: `go test ./internal/server/... -v -count=1` -- all three pass.
  </action>
  <verify>
    <automated>cd /root/workspace/one-codingplan && go test ./internal/server/... -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/server/server.go contains `func NewEngine() *gin.Engine`
    - internal/server/server.go contains `gin.New()`
    - internal/server/server.go contains `r.GET("/health"`
    - internal/server/server.go contains `"status": "ok"`
    - internal/server/server_test.go contains `TestHealthEndpoint_Status200`
    - internal/server/server_test.go contains `TestHealthEndpoint_JSONBody`
    - internal/server/server_test.go contains `TestHealthEndpoint_ContentType`
    - `go test ./internal/server/... -v -count=1` exits 0
  </acceptance_criteria>
  <done>Gin server package exports NewEngine() returning a configured engine with Logger, Recovery middleware, and GET /health endpoint returning 200 {"status":"ok"}. All three server tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: main.go — wire config + database + server</name>
  <files>cmd/ocp/main.go</files>
  <read_first>
    - internal/config/config.go (Load function signature, Config struct)
    - internal/database/database.go (Open, Migrate, SyncUpstreams signatures)
    - internal/server/server.go (NewEngine signature)
    - .planning/phases/01-project-skeleton-data-layer/01-RESEARCH.md (main.go Startup Sequence example)
  </read_first>
  <action>
    1. Create `cmd/ocp/main.go` with package `main`:
       - Import: `flag`, `fmt`, `log`, `one-codingplan/internal/config`, `one-codingplan/internal/database`, `one-codingplan/internal/server`
       - In `func main()`:
         a. Parse `--config` flag: `configPath := flag.String("config", "./config.yaml", "path to config file")`; call `flag.Parse()`
         b. Load config: `cfg, err := config.Load(*configPath)` -- fatal on error with `log.Fatalf("load config: %v", err)`
         c. Open database: `db, err := database.Open(cfg.Database.Path)` -- fatal on error with `log.Fatalf("open database: %v", err)`
         d. Migrate: `err = database.Migrate(db)` -- fatal on error with `log.Fatalf("migrate: %v", err)`
         e. Sync upstreams: `err = database.SyncUpstreams(db, cfg.Upstreams)` -- fatal on error with `log.Fatalf("sync upstreams: %v", err)`
         f. Create engine: `r := server.NewEngine()`
         g. Build address: `addr := fmt.Sprintf(":%d", cfg.Server.Port)`
         h. Log: `log.Printf("ocp starting on %s", addr)`
         i. Run: `if err := r.Run(addr); err != nil { log.Fatalf("server: %v", err) }`

    2. Create a `config.yaml` in the project root for local testing (this file is gitignored):
       ```yaml
       server:
         port: 8080
         admin_key: "dev-key"
       database:
         path: "./ocp.db"
       upstreams:
         - name: kimi
           base_url: https://api.moonshot.ai
           api_key: "test-key"
           enabled: true
       ```

    3. Verify compilation: `go build ./cmd/ocp/`

    4. Smoke test: start the binary in background, curl health endpoint, verify 200 response, kill process:
       ```
       go run ./cmd/ocp &
       sleep 2
       curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/health
       # expect: 200
       kill %1
       ```

    5. Verify SQLite database was created: `ls -la ocp.db` should exist.

    6. Run full test suite: `go test ./... -race -count=1` -- all tests pass with no race conditions.

    7. Clean up: `rm -f ocp.db` (test artifact).
  </action>
  <verify>
    <automated>cd /root/workspace/one-codingplan && go build ./cmd/ocp/ && go test ./... -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - cmd/ocp/main.go contains `flag.String("config", "./config.yaml"`
    - cmd/ocp/main.go contains `config.Load(*configPath)`
    - cmd/ocp/main.go contains `database.Open(cfg.Database.Path)`
    - cmd/ocp/main.go contains `database.Migrate(db)`
    - cmd/ocp/main.go contains `database.SyncUpstreams(db, cfg.Upstreams)`
    - cmd/ocp/main.go contains `server.NewEngine()`
    - cmd/ocp/main.go contains `r.Run(addr)`
    - `go build ./cmd/ocp/` exits 0
    - `go test ./... -race -count=1` exits 0
  </acceptance_criteria>
  <done>main.go wires config loading, DB open, migrate, upstream sync, and Gin server startup. Binary compiles. Full test suite passes with race detector. Binary serves GET /health with 200 when run with a valid config.yaml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config file -> main.go | Config path from --config flag; file read by Viper |
| HTTP client -> Gin server | External HTTP requests to /health endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Denial of Service | Gin /health endpoint | accept | Health endpoint is lightweight (no DB query); Gin's Recovery middleware prevents panics from crashing the process; rate limiting is a Phase 5 concern |
| T-01-06 | Tampering | --config flag path traversal | accept | Admin runs the binary; --config is not user-facing input; OS file permissions protect arbitrary file reads |
</threat_model>

<verification>
1. `go build ./cmd/ocp/` compiles without error
2. `go test ./... -race -count=1` -- all tests green, no races
3. Smoke test: `go run ./cmd/ocp` + `curl localhost:8080/health` returns 200 `{"status":"ok"}`
4. SQLite file created on first run with tables: upstreams, access_keys, usage_records
5. Upstream from config.yaml appears in SQLite after startup
</verification>

<success_criteria>
- Binary starts, loads config, creates DB, syncs upstreams, serves /health
- All tests pass with race detector enabled
- Phase 1 success criteria from ROADMAP.md are met (except SOCKS5 which is dropped per D-05)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-skeleton-data-layer/01-02-SUMMARY.md`
</output>
