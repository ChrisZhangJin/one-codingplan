---
phase: 01-project-skeleton-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - cmd/ocp/main.go
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/models/models.go
  - internal/database/database.go
  - internal/database/database_test.go
  - config.yaml.example
  - .gitignore
autonomous: true
requirements:
  - UPST-01
  - UPST-04
  - USGR-02

must_haves:
  truths:
    - "Go module initializes and all dependencies resolve"
    - "Config loads from YAML file with env var overrides"
    - "SQLite database file is created on first run with correct tables"
    - "Upstream entries from config are synced into SQLite"
    - "Database survives process restart (file-based persistence)"
  artifacts:
    - path: "go.mod"
      provides: "Go module definition"
      contains: "module one-codingplan"
    - path: "internal/config/config.go"
      provides: "Viper config loading with env overrides"
      exports: ["Load", "Config", "ServerConfig", "DatabaseConfig", "UpstreamConfig"]
    - path: "internal/models/models.go"
      provides: "GORM model structs for all three tables"
      contains: "type Upstream struct"
    - path: "internal/database/database.go"
      provides: "DB open, AutoMigrate, SyncUpstreams"
      exports: ["Open", "Migrate", "SyncUpstreams"]
    - path: "config.yaml.example"
      provides: "Canonical config schema reference"
      contains: "upstreams:"
  key_links:
    - from: "internal/database/database.go"
      to: "internal/models/models.go"
      via: "AutoMigrate uses model structs"
      pattern: "db\\.AutoMigrate"
    - from: "internal/database/database.go"
      to: "internal/config/config.go"
      via: "SyncUpstreams takes []UpstreamConfig"
      pattern: "config\\.UpstreamConfig"
---

<objective>
Scaffold the Go project, implement config loading (Viper + YAML), define GORM model structs, and build the database layer (SQLite open, AutoMigrate, upstream sync).

Purpose: Establishes the foundation that every subsequent phase imports -- module path, config schema, database access, and model definitions.
Output: Working Go module with config + database packages, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-skeleton-data-layer/01-CONTEXT.md
@.planning/phases/01-project-skeleton-data-layer/01-RESEARCH.md
@.planning/phases/01-project-skeleton-data-layer/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Go module + config package + model structs</name>
  <files>go.mod, go.sum, internal/config/config.go, internal/config/config_test.go, internal/models/models.go, config.yaml.example, .gitignore</files>
  <read_first>
    - .planning/phases/01-project-skeleton-data-layer/01-RESEARCH.md (config struct definitions, Viper patterns, model structs, version pins)
    - .planning/phases/01-project-skeleton-data-layer/01-CONTEXT.md (locked decisions D-01 through D-13)
    - CLAUDE.md (tech stack, library versions)
  </read_first>
  <behavior>
    - TestConfigLoad_FromFile: Create temp YAML with server.port=9090, database.path="/tmp/test.db", one upstream entry. Load it. Assert cfg.Server.Port == 9090, cfg.Database.Path == "/tmp/test.db", len(cfg.Upstreams) == 1, cfg.Upstreams[0].Name == "kimi".
    - TestConfigLoad_EnvOverride: Create temp YAML with server.port=8080. Set OCP_SERVER_PORT=9999. Load config. Assert cfg.Server.Port == 9999 (env overrides YAML). If Viper Unmarshal does not pick up env override automatically, use explicit BindEnv or post-Unmarshal override -- either approach is acceptable as long as the test passes.
    - TestConfigLoad_Defaults: Load config with minimal YAML (empty server block). Assert cfg.Server.Port == 8080 (default), cfg.Database.Path == "./ocp.db" (default).
    - TestConfigLoad_MissingFile: Call Load with nonexistent path. Assert error is returned.
  </behavior>
  <action>
    1. Initialize Go module (per D-01, Go >= 1.24):
       ```
       cd /root/workspace/one-codingplan
       go mod init one-codingplan
       ```
       Edit go.mod to set `go 1.24`.

    2. Create `.gitignore` with entries: `ocp.db`, `config.yaml`, `*.exe`, `/ocp`, `/dist/`.
       Create `config.yaml.example` with the canonical schema from RESEARCH.md:
       ```yaml
       server:
         port: 8080
         admin_key: "change-me"

       database:
         path: "./ocp.db"

       upstreams:
         - name: kimi
           base_url: https://api.moonshot.ai
           api_key: "sk-your-kimi-key"
           enabled: true
         - name: qwen
           base_url: https://dashscope.aliyuncs.com
           api_key: "sk-your-qwen-key"
           enabled: true
       ```

    3. Create `internal/models/models.go` with package `models` containing three GORM structs exactly as defined in RESEARCH.md Code Examples section:
       - `Upstream`: ID uint (primarykey, autoIncrement), CreatedAt, UpdatedAt, Name string (uniqueIndex, not null), BaseURL string (column:base_url, not null), APIKey string (column:api_key), Enabled bool (default:true)
       - `AccessKey`: ID string (primarykey), CreatedAt, UpdatedAt, Token string (uniqueIndex, not null), Enabled bool (default:true)
       - `UsageRecord`: ID uint (primarykey, autoIncrement), CreatedAt, KeyID string (index, not null), UpstreamID uint (index, not null), InputTokens int, OutputTokens int, LatencyMs int64, Success bool
       Import `time` for time.Time fields.

    4. Create `internal/config/config.go` with package `config`:
       - Define structs: `Config`, `ServerConfig`, `DatabaseConfig`, `UpstreamConfig` with mapstructure tags exactly as in RESEARCH.md Code Examples.
       - Implement `func Load(configPath string) (*Config, error)`:
         - Create new viper instance with `viper.New()`
         - Call `v.SetConfigFile(configPath)`
         - Set defaults: `v.SetDefault("server.port", 8080)`, `v.SetDefault("database.path", "./ocp.db")`
         - Call `v.ReadInConfig()` -- return error on failure
         - Call `v.SetEnvPrefix("OCP")`
         - Call `v.SetEnvKeyReplacer(strings.NewReplacer(".", "_"))`
         - Call `v.AutomaticEnv()`
         - Unmarshal into Config struct
         - After Unmarshal, explicitly override critical fields from Viper to handle env var edge case (per RESEARCH.md Pitfall 2):
           `cfg.Server.Port = v.GetInt("server.port")`
           `cfg.Server.AdminKey = v.GetString("server.admin_key")`
           `cfg.Database.Path = v.GetString("database.path")`
         - Return &cfg, nil

    5. Create `internal/config/config_test.go` implementing the four test cases from the behavior block above. Use `os.WriteFile` to create temp YAML files, `os.Setenv`/`os.Unsetenv` for env var tests.

    6. Install dependencies via goproxy.cn:
       ```
       GOPROXY=https://goproxy.cn go get github.com/gin-gonic/gin@v1.10.1 gorm.io/gorm@latest github.com/glebarez/sqlite@latest github.com/spf13/viper@latest github.com/google/uuid@latest
       ```
       Run `go mod tidy` after.

    7. Run tests: `go test ./internal/config/... -v -count=1` -- all four must pass.

    UPST-04 note: SOCKS5 support is dropped from scope entirely per user decision D-05. No httpclient package is created.
  </action>
  <verify>
    <automated>cd /root/workspace/one-codingplan && go test ./internal/config/... -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go.mod contains `module one-codingplan` and `go 1.24`
    - go.mod contains `github.com/gin-gonic/gin v1.10.1`
    - go.mod contains `gorm.io/gorm`
    - go.mod contains `github.com/glebarez/sqlite`
    - go.mod contains `github.com/spf13/viper`
    - internal/models/models.go contains `type Upstream struct`
    - internal/models/models.go contains `type AccessKey struct`
    - internal/models/models.go contains `type UsageRecord struct`
    - internal/config/config.go contains `func Load(configPath string) (*Config, error)`
    - internal/config/config.go contains `v.SetEnvPrefix("OCP")`
    - internal/config/config_test.go contains `TestConfigLoad_FromFile`
    - internal/config/config_test.go contains `TestConfigLoad_EnvOverride`
    - config.yaml.example contains `upstreams:` and `base_url:`
    - .gitignore contains `config.yaml` and `ocp.db`
    - `go test ./internal/config/... -v -count=1` exits 0
  </acceptance_criteria>
  <done>Go module initialized with all dependencies pinned. Config package loads YAML with env var overrides. Model structs define all three tables. All config tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Database package — open, migrate, sync upstreams</name>
  <files>internal/database/database.go, internal/database/database_test.go</files>
  <read_first>
    - internal/models/models.go (model struct definitions created in Task 1)
    - internal/config/config.go (UpstreamConfig struct used by SyncUpstreams)
    - .planning/phases/01-project-skeleton-data-layer/01-RESEARCH.md (Pattern 2: GORM Open, Pattern 3: AutoMigrate, Pattern 5: Config Sync upsert)
  </read_first>
  <behavior>
    - TestOpen: Call Open with a temp file path. Assert no error. Assert db is not nil.
    - TestAutoMigrate: Open in-memory DB. Call Migrate(db). Assert no error. Verify all three tables exist by running raw SQL: `SELECT name FROM sqlite_master WHERE type='table' AND name IN ('upstreams','access_keys','usage_records')` -- expect 3 rows.
    - TestSyncUpstreams_Create: Open in-memory DB, migrate. Call SyncUpstreams with two UpstreamConfig entries (name: "kimi", "qwen"). Query DB for Upstream records. Assert count == 2, names match.
    - TestSyncUpstreams_Update: Sync with kimi (base_url="https://old.url"). Sync again with kimi (base_url="https://new.url"). Query DB. Assert kimi's BaseURL == "https://new.url" and only 1 kimi record exists.
    - TestSyncUpstreams_Empty: Call SyncUpstreams with empty slice. Assert no error, no records created.
    - TestPersistence: Open DB with temp file path. Migrate. Sync one upstream. Close DB (get underlying sql.DB and close). Re-open same file. Query upstreams. Assert count == 1.
  </behavior>
  <action>
    1. Create `internal/database/database.go` with package `database`:
       - Import `gorm.io/gorm`, `github.com/glebarez/sqlite`, `gorm.io/gorm/clause`, `one-codingplan/internal/models`, `one-codingplan/internal/config`
       - `func Open(dbPath string) (*gorm.DB, error)`: call `gorm.Open(sqlite.Open(dbPath+"?_journal=WAL&_timeout=5000"), &gorm.Config{})`. Return db, err.
       - `func Migrate(db *gorm.DB) error`: call `db.AutoMigrate(&models.Upstream{}, &models.AccessKey{}, &models.UsageRecord{})`. Return error.
       - `func SyncUpstreams(db *gorm.DB, cfgUpstreams []config.UpstreamConfig) error`:
         - If len(cfgUpstreams) == 0, return nil
         - Build slice of models.Upstream from cfgUpstreams (map Name, BaseURL, APIKey, Enabled)
         - Call `db.Clauses(clause.OnConflict{Columns: []clause.Column{{Name: "name"}}, DoUpdates: clause.AssignmentColumns([]string{"base_url", "api_key", "enabled", "updated_at"})}).Create(&upstreams).Error`
         - Return error

    2. Create `internal/database/database_test.go` implementing all six test cases from the behavior block. Use `":memory:"` for in-memory tests and `os.CreateTemp` for persistence tests. For table existence check, use `db.Raw("SELECT name FROM sqlite_master WHERE type='table' AND name IN (?,?,?)", "upstreams", "access_keys", "usage_records").Scan(&names)`.

    3. Run tests: `go test ./internal/database/... -v -count=1` -- all six must pass.

    4. Run full suite: `go test ./internal/... -v -count=1` -- all tests (config + database) must pass.
  </action>
  <verify>
    <automated>cd /root/workspace/one-codingplan && go test ./internal/database/... -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/database/database.go contains `func Open(dbPath string) (*gorm.DB, error)`
    - internal/database/database.go contains `func Migrate(db *gorm.DB) error`
    - internal/database/database.go contains `func SyncUpstreams(db *gorm.DB, cfgUpstreams []config.UpstreamConfig) error`
    - internal/database/database.go contains `clause.OnConflict`
    - internal/database/database.go contains `_journal=WAL`
    - internal/database/database_test.go contains `TestOpen`
    - internal/database/database_test.go contains `TestAutoMigrate`
    - internal/database/database_test.go contains `TestSyncUpstreams_Create`
    - internal/database/database_test.go contains `TestSyncUpstreams_Update`
    - internal/database/database_test.go contains `TestPersistence`
    - `go test ./internal/database/... -v -count=1` exits 0
    - `go test ./internal/... -v -count=1` exits 0
  </acceptance_criteria>
  <done>Database package opens SQLite with WAL mode, creates all three tables via AutoMigrate, and syncs upstream config entries with upsert semantics. All database tests pass. Full internal test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config file -> application | YAML config contains API keys for upstream providers; read from filesystem |
| SQLite file -> application | Database file contains upstream API keys persisted from config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | config.yaml | mitigate | `.gitignore` includes `config.yaml`; provide `config.yaml.example` with placeholder values only; no real keys in example |
| T-01-02 | Information Disclosure | ocp.db (SQLite file) | mitigate | `.gitignore` includes `ocp.db`; document that file permissions should be 0600 in production (OS-level, not enforced in Go code at Phase 1) |
| T-01-03 | Information Disclosure | admin_key in config | accept | Default "change-me" value in example only; enforcement deferred to Phase 3/5 when auth middleware is added |
| T-01-04 | Spoofing | upstream base_url | accept | Config is admin-authored from local file; URL validation is low value since admin controls input; SSRF risk does not apply until relay pipeline (Phase 3) |
</threat_model>

<verification>
1. `go test ./internal/... -v -count=1` -- all tests green
2. `go test ./internal/... -race -count=1` -- no race conditions
3. go.mod contains pinned Gin v1.10.1 (not v1.12+)
4. All three GORM model structs exist with correct tags
5. Config loads from YAML and env overrides work
6. SQLite tables created and upstream sync works with upsert
</verification>

<success_criteria>
- Go module compiles with all dependencies
- Config tests pass (load, env override, defaults, missing file)
- Database tests pass (open, migrate, sync create/update, persistence)
- No race conditions detected
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-skeleton-data-layer/01-01-SUMMARY.md`
</output>
