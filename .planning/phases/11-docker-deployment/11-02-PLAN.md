---
phase: 11-docker-deployment
plan: 02
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - Dockerfile
autonomous: false
requirements: [DOCK-01, DOCK-02, DOCK-03, DOCK-04]
gap_closure: true

must_haves:
  truths:
    - "Dockerfile contains a valid Go image tag that exists on Docker Hub"
    - "docker build completes without error (human verified)"
    - "docker compose up starts the service and /health returns 200 (human verified)"
    - "database persists across container restarts (human verified)"
    - "docker compose run ocp-cli status works (human verified)"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage build with valid golang image tag"
      contains: "golang:1.25-alpine"
  key_links:
    - from: "Dockerfile"
      to: "Docker Hub golang image"
      via: "FROM tag in go-builder stage"
      pattern: "golang:1\\.25-alpine"
---

<objective>
Close gaps from 11-VERIFICATION.md: fix the broken Dockerfile image tag and provide a human verification checklist for runtime Docker tests that cannot be automated in this environment.

Purpose: The working tree Dockerfile regressed `golang:1.25-alpine` to the non-existent `golang:1.25.9-alpine`. This blocks `docker build`. Additionally, DOCK-02/03/04 require runtime Docker verification that the CI environment cannot perform.
Output: Fixed Dockerfile committed; human verification checklist for all four DOCK requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-docker-deployment/11-01-SUMMARY.md
@.planning/phases/11-docker-deployment/11-VERIFICATION.md
@Dockerfile
@docker-compose.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Dockerfile golang image tag (DOCK-01)</name>
  <files>Dockerfile</files>
  <read_first>
    - Dockerfile
  </read_first>
  <action>
On line 10 of the Dockerfile, change:

```
FROM golang:1.25.9-alpine AS go-builder
```

to:

```
FROM golang:1.25-alpine AS go-builder
```

The tag `golang:1.25.9-alpine` does not exist on Docker Hub. The committed version at 49f93de correctly used `golang:1.25-alpine`. This is the only change needed -- the other working tree changes (`node:24-slim` on line 2, `alpine:3.23.2` on line 20) are valid image updates and should be left as-is.

Do NOT modify any other lines in the Dockerfile. The ENTRYPOINT/CMD split (`ENTRYPOINT ["ocp"]` + `CMD ["serve"]`) is already correct from 11-01 execution.
  </action>
  <verify>
    <automated>grep -n "golang:" /root/workspace/one-codingplan/Dockerfile | grep -q "golang:1.25-alpine" && ! grep -q "golang:1.25.9" /root/workspace/one-codingplan/Dockerfile && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Line 10 of Dockerfile reads exactly `FROM golang:1.25-alpine AS go-builder`
    - No line in Dockerfile contains `golang:1.25.9`
    - Lines 1-9 and 11-27 are unchanged from the current working tree
  </acceptance_criteria>
  <done>Dockerfile go-builder stage uses a valid image tag that exists on Docker Hub</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Runtime Docker verification (DOCK-01, DOCK-02, DOCK-03, DOCK-04)</name>
  <files>none (verification only)</files>
  <action>
This is a human verification checkpoint. No code changes are made. The operator must run the four Docker tests below on a machine with Docker installed, since the CI/execution environment lacks a Docker daemon.
  </action>
  <what-built>
Phase 11 produced a complete Docker deployment configuration: Dockerfile (multi-stage build), docker-compose.yaml (service + CLI profiles), config.yaml.example, .env.example, and .dockerignore. Task 1 above fixed the broken image tag. All static configuration has been verified correct. Runtime verification requires a host with Docker installed.
  </what-built>
  <how-to-verify>
Run these four tests on a machine with Docker and Docker Compose installed. Each maps to a DOCK requirement.

**Setup (run once before testing):**
```bash
cd /path/to/one-codingplan
cp config.yaml.example config.yaml
# Edit config.yaml: set admin_key to a real value (not "change-me")
cat > .env << 'EOF'
OCP_ENCRYPTION_KEY=change-me-32-bytes-exactly-here
OCP_ADMIN_KEY=your-admin-key-here
EOF
mkdir -p data
```

**Test 1 -- DOCK-01: Docker Build**
```bash
docker build -t one-codingplan:latest .
```
Expected: Exits 0. All three stages (web-builder, go-builder, runtime) complete.

**Test 2 -- DOCK-02: Compose Up + Health**
```bash
docker compose up -d
sleep 5
curl -sf http://localhost:8080/health
```
Expected: Returns `{"status":"ok"}` with HTTP 200.

**Test 3 -- DOCK-03: Database Persistence**
```bash
docker compose down
docker compose up -d
sleep 5
curl -sf http://localhost:8080/health
ls -la ./data/ocp.db
```
Expected: Health returns 200 after restart. `./data/ocp.db` file exists on the host filesystem.

**Test 4 -- DOCK-04: CLI via Compose**
```bash
docker compose run --rm ocp-cli status
```
Expected: Exits 0. Prints upstream pool table with headers NAME, HEALTHY, POSITION, ENABLED.

**Cleanup:**
```bash
docker compose down
rm -f config.yaml .env
```
  </how-to-verify>
  <verify>
    <automated>echo "REQUIRES HUMAN — run docker build + compose tests on a Docker-capable host"</automated>
  </verify>
  <done>All four DOCK requirements confirmed working by human operator on a Docker-capable host</done>
  <resume-signal>Type "approved" if all 4 tests pass, or describe which test(s) failed and the error output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host filesystem to container | Config and data volumes mounted into container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | Dockerfile image tag | accept | Using minor-version tag (1.25-alpine) means auto-patch updates; acceptable for this project scale |
</threat_model>

<verification>
1. `grep "golang:1.25-alpine" Dockerfile` returns a match
2. `grep "golang:1.25.9" Dockerfile` returns no match
3. Human confirms all four DOCK tests pass on a Docker-capable host
</verification>

<success_criteria>
- DOCK-01: Dockerfile image tag is valid; docker build succeeds (human verified)
- DOCK-02: docker compose up serves /health with 200 (human verified)
- DOCK-03: database persists across container restarts (human verified)
- DOCK-04: ocp-cli status works via docker compose run (human verified)
</success_criteria>

<output>
After completion, create `.planning/phases/11-docker-deployment/11-02-SUMMARY.md`
</output>
